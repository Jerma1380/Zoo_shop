import 'package:flutter/material.dart';

class Note {

  final int id;
  final String title;
  final String textNote;
  final Color colorsNote;

  Note(this.id, this.title, this.textNote, this.colorsNote);
  
}